const bronMathPackage = require('bronislav_math_package');

const addResult = bronMathPackage.add(3, 9);
const subResult = bronMathPackage.substract(9, 3);

console.log(addResult);

console.log(subResult);